# 小马快跑项目规范

## 项目与目录

原生 JavaScript 静态逻辑解谜游戏，每行、列、颜色区域各一匹马，马之间八邻域不相邻。index.html 与 styles.css 为页面入口，game.js 为交互入口。src/ 存放可在 Node 与浏览器复用的纯逻辑；tests/ 存放回归测试；data/ 为已审计题库；scripts/ 为产题与审计工具；goals/ 为施工清单；docs/ 为设计与接口说明。临时产物仅存 .tmp/。

## 工作流

先读 ROADMAP.md 的当前检查点，再读对应 goals/ 清单。README.md 只写当前可运行功能。设计母稿为 docs/difficulty-design.md（实施前同步获批 v1.1）。新能力必须有对应证据，不能把旧文档的“已实现”声明当测试结果。

## 工程边界

保持原生 JavaScript 和静态文件打开能力。核心模块使用浏览器全局与 CommonJS 双导出。精确解数检查、人类推理和 UI 分工；提示引擎不能读取隐藏答案，用户 ✕ 不作为可信推理前提。每条证明可回放；超时返回 unknown，不等价无解。题库不得用不达标题静默回退。

## 验证与交付

纯逻辑使用 Node 内置测试；执行入口以 README.md 为准。主线资源必须经过独立唯一解检查、逐步证明验证与编排审查。UI 验证复用用户既有 Chrome 会话，不自行新建 Chrome 实例。真人试玩为独立验收门槛，自动化不能代替。不自行提交、发布或修改其他项目。
