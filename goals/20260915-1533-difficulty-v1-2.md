# v1.2 执行施工清单

创建：2026-09-15 15:33（北京时间）。用户已授权按 Goal 执行。

难度执行计划 v1.2（计划母稿已归档至本目录）。

## 目标与边界

2026-09-15 16:48 最新授权优先：用户明确“不试玩了，30 关之后继续增加难度”“先别研究四题包，先给完整 100 题，按照之前 30 关扩展，放一些类似 66 关比较难的”。停止 S1 候选表扩展、S2 四区提示修订与入门题研究；保留现有四题包文件但不继续研发。前 30 关题面、ID 和顺序保持；原第 31 关参考题保留样本或备份但不占正式主线。直接扩充为 100 关，31～100 的基线与难关逐步提高，难后回落，不用单纯加大棋盘代替推理。用户免除“先真人校准才扩关”的依赖；记录为未进行／用户取消，不伪造试玩通过。P3 前 30 关最终换位取消，本轮只审查并保留既有事实；P5 不再阻塞 P6。P2 无法连接浏览器时仍如实保留未验范围，不因此停产 100 题。本条覆盖旧计划对应依赖，Goal 不另建。

2026-09-15 15:58 试玩修订：过程完整但过长，P1 追加精简。页面保留关键候选、每步必要依据、矛盾与结论；前置排除合并，不做全文历史重放。完整证明数据及审计输出保留，既有按钮和棋盘不变。验收：第 18 关不超过 450 字符且保留第四列唯一候选、第五列两个候选各自被排除的原因；旋转、错误标记和全题库回归通过。

按 P0～P6 实施完整文字提示、关卡节奏校准、四区域题、真人试玩与 100 关扩充，部署到本机 http://localhost:8765/。保留原界面，提示仅做长文字所需调整。不引入分层播放、机关、服务端或外部发布，不提交 Git。真人反馈是最终编排和扩充的依赖，不能代为编造。

当前修改仅在本游戏项目内。临时搜索放 .tmp/difficulty-v1-2/，用户交付放当前 Codex 任务 outputs/。基线位于当前任务 work/baseline-v12-20260915-1533/。不覆盖其他任务修改，不删除用户存档。

## 检查点

- [x] P0：备份基线；同步文档；核对 30 关主线＋第 31 关参考题。
- [x] P1：从证明及必要前置步骤直接生成完整文字；第 18 关截图回归；长提示停留与失效。自动验证通过，真实视觉验收归 P2。
- [ ] P2：相关及全套 Node 测试；本机资源检查；既有 Chrome 内桌面／窄屏及操作验证。
- [x] P3 技术审查已交付；用户取消前 30 关最终换位，完整保留旧记录和已披露的问题，不再等待反馈重排。
- [x] P4：两种入口结构的四区域合格题、必要性证据及回落题；参考题族补不同答案骨架。内容及自动验收完成，实际难度校准归 P5、真实页面归 P2。
- P5：按用户 16:48 指令停止继续试玩校准，已有反馈保留；未进行的测试不记通过，不再作为 P6 前置。
- [x] P6 技术交付：实际 100 关入库，前 30 不变，新增 70 题唯一解／规则／证明／节奏验证及本机资源核对完成；真实渲染／触摸未验归 P2。

## 文件入口及接口决策

提示：src/hint-presenter.js 消费 proof、prerequisites 和 contradiction；不读取隐藏答案。game.js 管理当前提示的显示和失效；styles.css 只负责文字换行滚动。优先复用现有证明数据，不新增另一套解题算法。

题库：data/campaign.js；画像：src/difficulty.js；产题：scripts/search-four-region.cjs、scripts/search-66-like.cjs；独立裁判：src/puzzle-core.js。题库有变必须保存映射和版本，不套用旧格子状态。

## 测试与验收

node --test tests/*.test.cjs；node --check game.js；node --check src/hint-presenter.js。新增测试覆盖第 18 关事实与反证、错误叉号、旋转坐标、颜色映射、解释不读答案、过期提示与原操作。

四区：唯一解、无单格、有有效排除、三区上限停滞、四区允许可解、证明回放和独立结论验证。有限规则证据不声称覆盖所有人类解法。超时保留 unknown，不能算无解或通过。

浏览器检查与 HTTP、VM 测试分开记录；手机模拟与真实手机分开记录。任何未验项必须保留。所有要求及用户依赖均满足后才标记完整目标完成。

## 执行与交付

2026-09-15 17:21 受阻审计：100 关交付之后，连续三轮剩余 P2 均受既有 Chrome 连接阻塞。首轮 listBrowsers 成功、listTabs({browser:"1"}) 30 秒超时并重置内核；次轮核对工具清单，无其他可直接复用既有 Chrome 的验收接口；本轮复核施工清单与当前状态，无未依赖该连接的剩余施工，也无用户恢复连接通知。遵守不机械重试、不新建浏览器的约束，将原 Goal 标记 blocked，待连接恢复后继续真实页面／窄屏／操作／控制台验收。未把 HTTP 或 VM 冒充页面证据，100 关交付保持有效；已取消的试玩及四题包研究不恢复。

2026-09-15 17:15 100 关正式交付：版本 v1.2-local-100.1，100 连续关号与唯一 ID，前 30 记录与冻结基线深相等，70 新题不与旧题规范等价。14 对挑战后回落，普通中位分 40／48／56；实际 11 题有短反证，其中指定6关为66式短反证逻辑（不是声称全由截图变异）。新增70题560个方向、19,296个结论和9,013个提示完成验证。主 Agent 在两名 Sol 合并后独立跑全套71/71通过、语法及100条检查通过；17:14:25本机6个资源200且哈希匹配。题库SHA256为26f04fbaae598a7e77ae7f0c581e1af93178fa6cdee99833aca01085f04e1509。旧参考题保留在data/reference-level-66.json。存档按原始方向指纹恢复，升级/跳关先备份，不覆盖已有不同原档，备份失败阻止覆盖；胜败刷新、30→31、99→100及100结束均有VM回归。原UI与hint-presenter不变。已停止S1/S2研究。交付摘要：本任务outputs/campaign-100-execution.md；证据work/expand100/verification.json、delivery-evidence.json和verify-delivery.cjs。P2真实浏览器未验，原Goal未标完整完成；最新100关交付不再受已取消的真人校准阻塞。

2026-09-15 16:43 P5 首条四区域难度反馈：主人打开四题包后说“我第一关都感觉挺难”。据此撤销四题包第 1 题作为明确入门的建议，保留为挑战候选；四区域入门仍需更低负担候选。第 2／3 题及两者回落未获确认。已通知 Astra 修订入门规划、Sol 更新 S1 候选表；不改现有测试题或主线，不把单人反馈泛化为整体玩家结论，P5 保持未完成。

2026-09-15 16:39 A1 已由 Astra 完成只读规划，S1 已交 Sol 制作候选编排文档，不修改正在试玩的主线。P2 重新连接既有浏览器仍超时，不能用 HTTP 200 代替页面、窄屏或真实操作验收；已向主人请求四题包第 2→3 题的难度反馈，未代填结果。

2026-09-15 16:28 P4 内容交付：新四题包 `index.html#pack=four-pair` 包含原始四行关系、由五行候选经锁列缩到四行的隐藏挑战、同关系回落和不同答案骨架参考题。挑战 79→回落 63，证明深度 7→3，反证基础步数 3→0。32 个方向实例、2,336 个具体结论独立验证通过，49/49 全套测试通过；随后新增真实绕行反例，相关四项再次通过。正式搜索器已补准入门槛。三份存档相互隔离，主线未变；P2／P3／P5／P6 仍未完成。详见本任务 outputs/four-region-pair-test.md。

2026-09-15 16:16 P4 首个结构及 P3 审查交付：有约束边界修补得到 4 个近似四区候选，均通过 8 方向唯一解、四区准入与独立证明核验。其中 index 2 入选独立单题试玩 `index.html#pack=four`，不是主线第 1 关；独立存档不覆盖主线。45/45 测试通过，本机文件校验通过，真实浏览器未验。P3 新增全表审查并实际识别 3 个反向回落。P4 仍缺第二种结构、配套回落和参考题答案多样性；P5 未收到难度校准，P6 未启动。交付见本任务 outputs/four-region-first-test.md。

2026-09-15 16:01 反馈修订验收：主人实际阅读后认为“过程完整，但文字太长”。第 18 关页面文本已从 1,799 字符缩到 250 字符，前置排除概括为第四／五列的候选，保留三步关键依据；完整证明仍可审计。39/39 测试再次通过，8765 HTTP 200 且新提示脚本哈希与磁盘一致。P5 只收到文字反馈，不等于难度校准通过；P2 真实浏览器和 P3～P6 未完事项保持。

2026-09-15 15:51 首批验收：P0／P1 完成。39/39 Node 测试通过，语法检查通过，本机 8765 的脚本和样式与磁盘哈希一致。当前 31 条题目全部重新分析通过；第 24、26、29 关回落关系需修正。CUA 读取既有 Chrome 超时，P2 真实页面仍未验。P4 两轮搜索加边界修补尚未得到满足必要性要求的题，未接入。P5 等待主人反馈，P6 未启动。完整交付记录在当前 Codex 任务 outputs/difficulty-v12-checkpoint.md。

## Astra 规划、Sol 执行分工

S3／S4 并行写入分离：S3 的 Sol 仅负责生产脚本、题库数据、题库及生产测试、README/dataREADME；不再修改 game.js、progress-store.js 或其测试。S4 新 Sol 仅负责 game.js、src/progress-store.js、tests/progress-store.test.cjs 和新 tests/campaign-resume.test.cjs，不写题库／生成脚本／文档／其他测试。共享接口固定：campaign.resumeCompatibility = {campaignVersion: 旧版本, records: [{level, puzzleId, fingerprint}]}，仅冻结前 30 条；fingerprint 为 JSON.stringify({size:p.size,regions:p.regions,solution:p.solution,colorMap:p.colorMap||null})，禁止旋转归一化。S3 从冻结基线生成此元数据；S4 读取它完成旧局迁移，正式新存档另存 puzzleFingerprint。双方不能覆盖其他工作者修改，不嵌套委派。S4 验收须覆盖同题、旧参考31、未知版本、同ID异题指纹、备份失败不覆盖原档、损坏存档、两种试玩存档隔离、末关不越界和下一关真实尺寸。S3 不因S4测试尚未合入放宽生产验收，最终全套测试由主 Agent 在两者交付后执行。

S3／100 关交付：Astra 只读制定生产规格、编排曲线、存档兼容与测试；Sol 按已审查规格执行。入口为 data/campaign.js、现有 scripts/puzzle-source.cjs、search-66-like.cjs、generate-puzzles.cjs、difficulty.js、logic-engine.js、game.js、progress-store.js 及 tests。允许 Sol 修改专用构建／审计脚本、100 关题库、必要的主线存档兼容、对应回归和 README；不改四题包、UI、规则、经济系统、服务配置，不提交或外部发布、不委派。主 Agent 独占 Goal／ROADMAP／用户交付摘要，避免并发写。生产中间证据存任务 work/ 专用目录。先保存当前题库与必要基线；采用确定性种子、对称归一化去重，不复制同题换色填数；新题唯一解、连通、零单格、允许推理可解、提示可生成、证明可回放，unknown 不入库。验收实际 100 条连续关号与唯一 ID、前 30 条不变、70 道新题独立审计、难后回落和渐进曲线、旧存档合法恢复／不同题不套用旧标记、100 关边界及缺资源失败路径。本机 8765 资源一致后交付，真实浏览器未验如实列明。

2026-09-15 16:47 S2／Sol 执行 Astra 的提示定位修订：用户已明确卡在“看不出该观察哪四个区域”。仅改 src/hint-presenter.js 的精简四区域联合解释和对应测试，先写颜色主体、目标行列、占满原因，最后写全部排除结论，移除主体与关系之间的候选坐标长串。所有信息来自 proof 与 colorMap；有前置排除须准确说明，保留完整证明和前提校验，不读取答案，不改题库、UI、存档、服务，不搜索或扩关。验收覆盖原始四区、隐藏四区前提、行列转置、颜色映射、缺失前提、不读答案和第 18 关精简回归；主 Agent 审查实际补丁、测试和本机资源。现有 S1 文档可完成当前版本但不继续扩展；该小修优先交付。真人仅可确认提示辅助定位，不能据此勾选独立入门或回落。

用户指定模型分工，采用当前任务内原生子智能体顺序交接；不更改全局模型配置，不另建用户任务。

- A1／Astra：只读核对本清单、ROADMAP.md、现有 v1.2 执行计划、四题包交付及全表节奏审计入口；规划 P3 尚可独立推进的准备工作。交付一份供 Sol 执行的精确任务说明：候选映射与回落问题处理建议、数据入口、步骤和验收方式。不得擅自决定真人难度已通过，不改主线或存档。
- S1／Sol：依赖 A1 经主 Agent 核对的计划，读取实际题库与审计结果，形成 outputs/campaign-rhythm-next-step.md，包含旧关号／题目 ID、候选位置、回落对应及未决项。只允许修改此交付文档及 work/ 下专用证据文件；不改业务代码、题库、界面、存档或服务，不提交或发布，不嵌套委派。
- 验收：主 Agent 核对所有映射可追溯至实际题库；明确区分方案与已部署事实；缺失候选或 unknown 如实保留，不能强行凑足 30 或 100 关。P3 最终编排与 P5 仍待试玩反馈。主 Agent 同步本清单和 ROADMAP.md 的真实状态。

每阶段记录真实文件、检查结果、未完成事项；交付可试玩版本、审计与题目 ID／关号映射。未收到试玩不能勾选 P5，100 关未入库不能勾选 P6。
