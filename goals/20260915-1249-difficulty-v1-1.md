# v1.1 难度与提示改造施工清单

创建：2026-09-15 12:49（北京时间）。状态：G1、G2 技术试验已验收，G3 第二版本地接入已验收，G4 待真实页面验证。2026-09-15 13:29 Goal 工具返回无活动目标，未自动重建；技术验收与平台 Goal 状态分开记录。

## 目标及边界

执行已批准的 v1.1 实施计划（计划母稿已归档至本目录）。

按依赖拆成独立 Goal，逐项验收：G1 正确性与证明底座（P0/P1）；G2 难度画像与产题试验（P2）；G3 固定 30 关与存档（P3）；G4 提示展示与完整回归（P4）；G5 真人试玩及校准后扩充（P5）。一次只执行一个 Goal，不以技术测试代替真人验收。

修改仅限本游戏目录；不提交、推送或部署，不访问外部账户。保留现有玩法与操作；不引入框架、数据库、LLM 提示、广告、强制限时和动态难度。保留旧文件备份，状态切换与内容版本明确。

## G1：正确性与证明底座

- [x] 冻结原始代码、提示错误 fixture，记录 SHA-256。
- [x] 建立项目规范、README、ROADMAP 与 v1.1 设计入口。
- [x] 独立验证尺寸、区域连通、行列、区域单马与相邻限制。
- [x] 实现有预算的精确解数检查，区分 0、1、2+ 与 unknown。
- [x] 共享推理引擎覆盖唯一候选、已放马排除、双向锁、2～4 区域联合、共同冲突与单层限定反证。
- [x] ProofStep 包含前提、来源格、结论格、矛盾对象和可回放步骤。
- [x] 手动 ✕ 不污染推理；旧错误样例不再错误排除马。
- [x] Node 回归包括无解、多解、唯一解、非法输入、规则反例及每步独立裁判检查。

G1 交付：纯逻辑模块、测试与 fixture、测试结果、当前状态文档。验收仅覆盖底座，暂不声称关卡与 UI 已改造。

2026-09-15 13:08 验收：21 项 Node 测试通过，随机局面独立核对 1,934 个推理步骤，另有 k=2～4 的双向／转置正例与候选越界反例。经典脚本 VM 接入通过；错误标记 fixture 返回纠错，不再排除马。浏览器布局与真实操作留至 G4，不以 VM 替代。

## G2：难度画像与产题

实现最低规则层级、瓶颈、推进段、版本固定评分及对称去重。产出零单格舒缓、单关键短挑战、多瓶颈长挑战和同技巧配对；完成至少 1,000 固定种子候选及变换审计。未命中目标时记录失败，不静默放宽。

2026-09-15 13:27 验收：difficulty.js、离线题源及审计工具完成；1,000 个种子中 999 个通过、1 个拒绝，8,000 个变换与 50,951 个结论核对通过。另选 30 道开发样本、6 对同尺寸同技巧题，并复核 240 个变换与 516 个证明步骤。定向四区构造没有合格产出，明确记为 G3 槽位缺口，不把样本包视为正式主线。详情 docs/production-audit.md。

## G3：30 关与存档

按 v1.1 前 30 关教学习序与波浪配置固定选题；挑战后同技巧巩固，第 9 关后零单格，关卡大小独立。重玩同题，刷新恢复，损坏与旧版本处理明确；无题库时显式报错。

- [x] 关卡规格与内容准入校验：30 关固定资源全部唯一可解、可由允许规则解完，并有缺资源时的显式错误。
- [x] 进度模块：严格校验、同题恢复、错误内容不直接信任；本轮保存核心进度与棋盘状态，辅助使用记录暂不扩展。
- [x] 游戏接入：重玩同题、刷新恢复、旧交互状态重置。
- [x] 首 30 关正式选题：采用已审查的单／两／三区域和反证题；四区域必要题作为后续题源，不降低准入标准硬塞入主线。
- [x] 自动验收与真实页面验证分开记录。

2026-09-15 14:29 验收：`data/campaign.js` 固定 30 关已接入 `game.js`；挑战后回落断言、唯一解、允许规则求解和证明回放通过。`src/progress-store.js` 通过有效中途局面恢复、冲突数组和损坏 JSON 测试。真实页面验证转入 G4。
2026-09-15 15:04 第二版验收：从用户第 66 关截图转录 7×7 区域骨架，确认零单格、唯一解、深埋突破和 5 层限定反证；基于该骨架搜索出 3 道 76～82 分变体，替换第 18、20、27 关，分别保留第 19、21、28 关回落。32 项 Node 测试全部通过，本机 HTTP 资源已加载 `v1.1-local-30.4`。

## G4：提示与 UI

2026-09-15 15:33：本节旧三层交互方案已由用户简化为“直接写清完整推理过程”。后续按 20260915-1533-difficulty-v1-2.md 执行，不再实施下述三层展示。

三层提示、证据与结论分开高亮、行列标识、反证逐步解释、纠错、过期提示失效；默认提示不落子。验证原操作、重玩、存档、无控制台错误及手机／桌面布局。复用既有 Chrome 会话；受阻时保留未验项。

当前状态：提示逻辑和高亮目标已接入；提示卡的视觉分层、真实页面交互和控制台检查待主人本机试玩及可用浏览器会话后完成。界面整体不做重设计。

## G5：真人试玩与校准

准备同尺寸盲测与完整主线体验两个任务块，先交主人试玩。6～10 名玩家招募、真实评价及返回数据需要用户实际参与；没有这些证据不能宣称完成 P5。校准后再扩 100 关。

## 执行纪律

当前由主 Agent 顺序实现，没有派发子任务。若后续委派，先在本清单补目标、文件入口、依赖、修改范围、接口、失败路径、验收和交付，再派发编号。每一 Goal 完成后更新 ROADMAP 和本清单；主 Agent 核对实际证据后才标记完成。
